export default class ObjectManager {
    constructor(logger) {
        this.logger = logger;
        this.objects = {}
    }

    insert(objectId) {
        this.objects[objectId];
    }

    update(objectId, changes) {
        this.objects[objectId];
    }

    remove(objectId) {
        this.objects[objectId];
    }
}